import {describe,expect,it} from 'vitest'; import {formatBasisPoints,formatCurrency,formatPercent,formatYield,freshnessLabel} from './index';
describe('financial formatting',()=>{it('uses finance-specific units and signs',()=>{expect(formatYield(11.234)).toBe('11.23%');expect(formatBasisPoints(-4)).toBe('-4.0 bp');expect(formatPercent(0.4)).toBe('+0.40%')});it('renders missing values without false precision',()=>expect(formatYield(null)).toBe('—'));it('formats currencies and freshness',()=>{expect(formatCurrency(100,'PKR')).toContain('100.00');expect(freshnessLabel('DELAYED',15)).toBe('DELAYED 15M')})});
